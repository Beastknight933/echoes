# web framework
fastapi==0.100.0
pydantic==2.4.0
uvicorn[standard]==0.22.0

# core numeric / ML
numpy==1.25.3

# scikit-learn (works fine with numpy 1.25.x)
scikit-learn==1.3.2

# sentence-transformers and its cores
sentence-transformers==2.2.2
transformers==4.41.0
torch==2.1.2     # CPU or GPU wheel depending on your platform (see notes)
